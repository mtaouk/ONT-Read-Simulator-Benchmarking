---
title: "ONT_sim"
author: "Mona Taouk"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(tidyverse)
library(scales)
library(cowplot)

data <- read.csv("results/all_tools_stats.tsv", sep = "\t")

data$Identity_Qscore <- -10 * log10(1 - data$actual_identity)
data$Identity_Qscore <- pmin(data$Identity_Qscore, 50)

data <- data %>%
  mutate(tool = recode(
    tool,
    "simlord" = "SimLoRD",
    "real_reads" = "Real reads"
  ))

df_Badread <- data %>% filter(tool == "Badread")
df_LongISLND <- data %>% filter(tool == "LongISLND")
df_lrsim <- data %>% filter(tool == "lrsim")
df_NanoSim <- data %>% filter(tool == "NanoSim")
df_PBSIM3 <- data %>% filter(tool == "PBSIM3")
df_SimLoRD <- data %>% filter(tool == "SimLoRD")
df_Real_reads <- data %>% filter(tool == "Real reads")
```

```{r colours}
badread_colour <- "#B96762"
longislnd_colour <- "#8E7F00"
lrsim_colour <- "#269049"
nanosim_colour <- "#009295"
pbsim3_colour <- "#5F7DBE"
simlord_colour <- "#B460A8"
real_reads_colour <- "#7E7E7E"
reference_line_colour <- "grey70"

simulator_colours <- c(
  "Badread" = badread_colour,
  "LongISLND" = longislnd_colour,
  "lrsim" = lrsim_colour,
  "NanoSim" = nanosim_colour,
  "PBSIM3" = pbsim3_colour,
  "SimLoRD" = simlord_colour
)

adjust_figure_4_colours <- function(colour, luminance_scale = 0.3, chroma_scale = 1.25) {
  hcl_values <- colorspace::coords(
    as(colorspace::hex2RGB(colour), "polarLUV")
  )

  hcl_values[, "L"] <- hcl_values[, "L"] * luminance_scale
  hcl_values[, "C"] <- hcl_values[, "C"] * chroma_scale

  adjusted <- colorspace::hex(
    colorspace::polarLUV(
      L = hcl_values[, "L"],
      C = hcl_values[, "C"],
      H = hcl_values[, "H"]
    ),
    fixup = TRUE
  )

  stats::setNames(adjusted, names(colour))
}

figure_4_colours <- adjust_figure_4_colours(c(
  "Badread" = badread_colour,
  "LongISLND" = longislnd_colour,
  "lrsim" = lrsim_colour,
  "NanoSim" = nanosim_colour,
  "PBSIM3" = pbsim3_colour,
  "SimLoRD" = simlord_colour,
  "Real reads" = real_reads_colour
))
```

```{r themes}
histogram_panel_theme <- theme_bw() + theme(
  axis.text.y = element_blank(),
  axis.ticks.y = element_blank(),
  plot.title = element_text(hjust = 0.5, size = 9),
  plot.margin = margin(2, 2, 2, 2),
  panel.background = element_rect(fill = "white"),
  panel.grid.major.y = element_blank(),
  panel.grid.minor.y = element_blank()
)

histogram_hidden_x_theme <- histogram_panel_theme + theme(
  axis.text.x = element_text(colour = "white"),
  axis.ticks.x = element_line(colour = "white")
)

histogram_visible_x_theme <- histogram_panel_theme + theme(
  axis.text.x = element_text()
)

scatter_panel_theme <- theme_bw() + theme(
  plot.title = element_text(hjust = 0.5, size = 9),
  plot.margin = margin(2, 2, 2, 2),
  panel.background = element_rect(fill = "white")
)

scatter_hidden_x_theme <- scatter_panel_theme + theme(
  axis.text.x = element_text(colour = "white"),
  axis.ticks.x = element_line(colour = "white")
)

scatter_hidden_x_hidden_y_theme <- scatter_hidden_x_theme + theme(
  axis.text.y = element_blank(),
  axis.ticks.y = element_blank()
)

scatter_visible_x_theme <- scatter_panel_theme + theme(
  axis.text.x = element_text()
)

scatter_visible_x_hidden_y_theme <- scatter_visible_x_theme + theme(
  axis.text.y = element_blank(),
  axis.ticks.y = element_blank()
)

supplementary_facet_theme <- theme_bw() + theme(
  legend.position = "none",
  strip.background = element_blank(),
  axis.text.x = element_text(),
  axis.text.y = element_blank(),
  axis.ticks.y = element_blank(),
  plot.margin = margin(2, 2, 2, 2),
  panel.grid.major.y = element_blank(),
  panel.grid.minor.y = element_blank()
)

supplementary_facet_no_y_grid_theme <- supplementary_facet_theme + theme(
  panel.grid.major.y = element_blank(),
  panel.grid.minor.y = element_blank()
)

supplementary_calibration_theme <- theme_bw() + theme(
  legend.position = "none",
  strip.background = element_blank(),
  axis.text.x = element_text(),
  plot.margin = margin(2, 2, 2, 2)
)

supplementary_real_calibration_theme <- theme_bw() + theme(
  axis.text.x = element_text(),
  plot.title = element_text(hjust = 0.5, size = 9),
  strip.background = element_blank(),
  plot.margin = margin(2, 2, 2, 2)
)

per_base_calibration_plot <- function(data, colour, title, x_label = "", y_label = "", plot_theme) {
  ggplot(data, aes(x = qscore, y = error_qscore)) +
    geom_abline(intercept = 0, slope = 1, linetype = "dashed", colour = reference_line_colour) +
    geom_point(colour = colour, alpha = 0.8, size = 2) +
    labs(x = x_label, y = y_label, title = title) +
    scale_x_continuous(limits = c(0, max_q)) +
    scale_y_continuous(limits = c(0, max_q)) +
    plot_theme
}
```

```{r plot_scales}
read_length_x_scale <- function() {
  scale_x_continuous(
    labels = label_number(scale = 1 / 1000),
    limits = c(0, 100000)
  )
}

identity_qscore_x_scale <- function() {
  scale_x_continuous(limits = c(0, 50))
}

mean_qscore_x_scale <- function() {
  scale_x_continuous(limits = c(0, 50))
}

identity_qscore_y_scale <- function() {
  scale_y_continuous(limits = c(0, 50))
}

supplementary_qscore_x_scale <- function() {
  scale_x_continuous(limits = c(0, 80))
}

count_y_scale <- function() {
  scale_y_continuous(
    limits = c(0, NA),
    expand = expansion(mult = c(0, 0.05))
  )
}
```

```{r layouts}
add_figure_title <- function(plot, title) {
  plot_grid(
    ggdraw() + draw_label(title, x = 0.5, hjust = 0.5),
    plot,
    ncol = 1,
    rel_heights = c(0.05, 1)
  )
}

make_tool_matrix <- function(plots, title, align, axis) {
  add_figure_title(
    plot_grid(
      plotlist = plots,
      ncol = 3,
      align = align,
      axis = axis
    ),
    title
  )
}
```

```{r}
plot_Badread <- ggplot(df_Badread, aes(x = read_length)) +
  geom_histogram(aes(weight = read_length), binwidth = 1000, position = "identity", fill = badread_colour, colour = badread_colour, linewidth = 0.15) +
  read_length_x_scale() +
  count_y_scale() +
  labs(x = "", y = "", title = "Badread") +
  histogram_hidden_x_theme

plot_LongISLND <- ggplot(df_LongISLND, aes(x = read_length)) +
  geom_histogram(aes(weight = read_length), binwidth = 1000, position = "identity", fill = longislnd_colour, colour = longislnd_colour, linewidth = 0.15) +
  read_length_x_scale() +
  count_y_scale() +
  labs(x = "", y = "", title = "LongISLND") +
  histogram_hidden_x_theme

plot_lrsim <- ggplot(df_lrsim, aes(x = read_length)) +
  geom_histogram(aes(weight = read_length), binwidth = 1000, position = "identity", fill = lrsim_colour, colour = lrsim_colour, linewidth = 0.15) +
  read_length_x_scale() +
  count_y_scale() +
  labs(x = "", y = "Count", title = "lrsim") +
  histogram_hidden_x_theme

plot_NanoSim <- ggplot(df_NanoSim, aes(x = read_length)) +
  geom_histogram(aes(weight = read_length), binwidth = 1000, position = "identity", fill = nanosim_colour, colour = nanosim_colour, linewidth = 0.15) +
  read_length_x_scale() +
  count_y_scale() +
  labs(x = "", y = "", title = "NanoSim") +
  histogram_hidden_x_theme

plot_PBSIM3 <- ggplot(df_PBSIM3, aes(x = read_length)) +
  geom_histogram(aes(weight = read_length), binwidth = 1000, position = "identity", fill = pbsim3_colour, colour = pbsim3_colour, linewidth = 0.15) +
  read_length_x_scale() +
  count_y_scale() +
  labs(x = "", y = "", title = "PBSIM3") +
  histogram_visible_x_theme

plot_SimLoRD <- ggplot(df_SimLoRD, aes(x = read_length)) +
  geom_histogram(aes(weight = read_length), binwidth = 1000, position = "identity", fill = simlord_colour, colour = simlord_colour, linewidth = 0.15) +
  read_length_x_scale() +
  count_y_scale() +
  labs(x = "Read length (kbp)", y = "", title = "SimLoRD") +
  histogram_visible_x_theme

plot_real <- ggplot(df_Real_reads, aes(x = read_length)) +
  geom_histogram(aes(weight = read_length), binwidth = 1000, position = "identity", fill = real_reads_colour, colour = real_reads_colour, linewidth = 0.15) +
  read_length_x_scale() +
  count_y_scale() +
  labs(x = "", y = "", title = "Real reads") +
  histogram_visible_x_theme

figure_1 <- make_tool_matrix(
  plots = list(
    plot_Badread, plot_LongISLND, NULL,
    plot_lrsim, plot_NanoSim, plot_real,
    plot_PBSIM3, plot_SimLoRD, NULL
  ),
  title = "Read length distribution for each simulation tool",
  align = "h",
  axis = "b"
)

ggsave(
  "figures/figure_1.pdf",
  figure_1,
  height  = 160,
  width = 200,
  units  = "mm",
  device = "pdf"
)

figure_1
```

```{r}
plot_Badread <- ggplot(df_Badread, aes(x = Identity_Qscore)) +
  geom_histogram(binwidth = 1, position = "identity", fill = badread_colour, colour = badread_colour, linewidth = 0.15) +
  identity_qscore_x_scale() +
  count_y_scale() +
  labs(x = "", y = "", title = "Badread") +
  histogram_hidden_x_theme

plot_LongISLND <- ggplot(df_LongISLND, aes(x = Identity_Qscore)) +
  geom_histogram(binwidth = 1, position = "identity", fill = longislnd_colour, colour = longislnd_colour, linewidth = 0.15) +
  identity_qscore_x_scale() +
  count_y_scale() +
  labs(x = "", y = "", title = "LongISLND") +
  histogram_hidden_x_theme

plot_lrsim <- ggplot(df_lrsim, aes(x = Identity_Qscore)) +
  geom_histogram(binwidth = 1, position = "identity", fill = lrsim_colour, colour = lrsim_colour, linewidth = 0.15) +
  identity_qscore_x_scale() +
  count_y_scale() +
  labs(x = "", y = "Count", title = "lrsim") +
  histogram_hidden_x_theme

plot_NanoSim <- ggplot(df_NanoSim, aes(x = Identity_Qscore)) +
  geom_histogram(binwidth = 1, position = "identity", fill = nanosim_colour, colour = nanosim_colour, linewidth = 0.15) +
  identity_qscore_x_scale() +
  count_y_scale() +
  labs(x = "", y = "", title = "NanoSim") +
  histogram_hidden_x_theme

plot_PBSIM3 <- ggplot(df_PBSIM3, aes(x = Identity_Qscore)) +
  geom_histogram(binwidth = 1, position = "identity", fill = pbsim3_colour, colour = pbsim3_colour, linewidth = 0.15) +
  identity_qscore_x_scale() +
  count_y_scale() +
  labs(x = "", y = "", title = "PBSIM3") +
  histogram_visible_x_theme

plot_SimLoRD <- ggplot(df_SimLoRD, aes(x = Identity_Qscore)) +
  geom_histogram(binwidth = 1, position = "identity", fill = simlord_colour, colour = simlord_colour, linewidth = 0.15) +
  identity_qscore_x_scale() +
  count_y_scale() +
  labs(
    x = "Mean identity-derived quality score (per read)",
    y = "",
    title = "SimLoRD"
  ) +
  histogram_visible_x_theme

plot_real <- ggplot(df_Real_reads, aes(x = Identity_Qscore)) +
  geom_histogram(binwidth = 1, position = "identity", fill = real_reads_colour, colour = real_reads_colour, linewidth = 0.15) +
  identity_qscore_x_scale() +
  count_y_scale() +
  labs(x = "", y = "", title = "Real reads") +
  histogram_visible_x_theme

figure_2 <- make_tool_matrix(
  plots = list(
    plot_Badread, plot_LongISLND, NULL,
    plot_lrsim, plot_NanoSim, plot_real,
    plot_PBSIM3, plot_SimLoRD, NULL
  ),
  title = "Mean identity-derived quality score per read for each tool",
  align = "h",
  axis = "b"
)

ggsave(
  "figures/figure_2.pdf",
  figure_2,
  height  = 160,
  width = 200,
  units  = "mm",
  device = "pdf"
)

figure_2
```

```{r}
plot_Badread <- ggplot(df_Badread, aes(x = mean_qscore)) +
  geom_histogram(binwidth = 1, position = "identity", fill = badread_colour, colour = badread_colour, linewidth = 0.15) +
  mean_qscore_x_scale() +
  count_y_scale() +
  labs(x = "", y = "", title = "Badread") +
  histogram_hidden_x_theme

plot_LongISLND <- ggplot(df_LongISLND, aes(x = mean_qscore)) +
  geom_histogram(binwidth = 1, position = "identity", fill = longislnd_colour, colour = longislnd_colour, linewidth = 0.15) +
  mean_qscore_x_scale() +
  count_y_scale() +
  labs(x = "", y = "", title = "LongISLND") +
  histogram_hidden_x_theme

plot_lrsim <- ggplot(df_lrsim, aes(x = mean_qscore)) +
  geom_histogram(binwidth = 1, position = "identity", fill = lrsim_colour, colour = lrsim_colour, linewidth = 0.15) +
  mean_qscore_x_scale() +
  count_y_scale() +
  labs(x = "", y = "Count", title = "lrsim") +
  histogram_hidden_x_theme

plot_NanoSim <- ggplot(df_NanoSim, aes(x = mean_qscore)) +
  geom_histogram(binwidth = 1, position = "identity", fill = nanosim_colour, colour = nanosim_colour, linewidth = 0.15) +
  mean_qscore_x_scale() +
  count_y_scale() +
  labs(x = "", y = "", title = "NanoSim") +
  histogram_hidden_x_theme

plot_PBSIM3 <- ggplot(df_PBSIM3, aes(x = mean_qscore)) +
  geom_histogram(binwidth = 1, position = "identity", fill = pbsim3_colour, colour = pbsim3_colour, linewidth = 0.15) +
  mean_qscore_x_scale() +
  count_y_scale() +
  labs(x = "", y = "", title = "PBSIM3") +
  histogram_visible_x_theme

plot_SimLoRD <- ggplot(df_SimLoRD, aes(x = mean_qscore)) +
  geom_histogram(binwidth = 1, position = "identity", fill = simlord_colour, colour = simlord_colour, linewidth = 0.15) +
  mean_qscore_x_scale() +
  count_y_scale() +
  labs(x = "Mean quality score (per read)", y = "", title = "SimLoRD") +
  histogram_visible_x_theme

plot_real <- ggplot(df_Real_reads, aes(x = mean_qscore)) +
  geom_histogram(binwidth = 1, position = "identity", fill = real_reads_colour, colour = real_reads_colour, linewidth = 0.15) +
  mean_qscore_x_scale() +
  count_y_scale() +
  labs(x = "", y = "", title = "Real reads") +
  histogram_visible_x_theme

figure_3 <- make_tool_matrix(
  plots = list(
    plot_Badread, plot_LongISLND, NULL,
    plot_lrsim, plot_NanoSim, plot_real,
    plot_PBSIM3, plot_SimLoRD, NULL
  ),
  title = "Mean quality score for each tool",
  align = "h",
  axis = "b"
)

ggsave(
  "figures/figure_3.pdf",
  figure_3,
  height  = 160,
  width = 200,
  units  = "mm",
  device = "pdf"
)

figure_3
```

```{r}
figure_4_point_layer <- function(colour) {
  ggrastr::geom_point_rast(
    alpha = 0.05,
    colour = colour,
    shape = 16,
    size = 0.1,
    raster.dpi = 600,
    dev = "ragg"
  )
}

plot_Badread <- ggplot(df_Badread, aes(x = mean_qscore, y = Identity_Qscore)) +
  figure_4_point_layer(figure_4_colours[["Badread"]]) +
  mean_qscore_x_scale() +
  identity_qscore_y_scale() +
  labs(x = "", y = "", title = "Badread") +
  scatter_hidden_x_theme

plot_LongISLND <- ggplot(df_LongISLND, aes(x = mean_qscore, y = Identity_Qscore)) +
  figure_4_point_layer(figure_4_colours[["LongISLND"]]) +
  mean_qscore_x_scale() +
  identity_qscore_y_scale() +
  labs(x = "", y = "", title = "LongISLND") +
  scatter_hidden_x_hidden_y_theme

plot_lrsim <- ggplot(df_lrsim, aes(x = mean_qscore, y = Identity_Qscore)) +
  figure_4_point_layer(figure_4_colours[["lrsim"]]) +
  mean_qscore_x_scale() +
  identity_qscore_y_scale() +
  labs(x = "",  y = "Mean identity-derived\nquality score (per read)", title = "lrsim") +
  scatter_hidden_x_theme

plot_NanoSim <- ggplot(df_NanoSim, aes(x = mean_qscore, y = Identity_Qscore)) +
  figure_4_point_layer(figure_4_colours[["NanoSim"]]) +
  mean_qscore_x_scale() +
  identity_qscore_y_scale() +
  labs(x = "", y = "", title = "NanoSim") +
  scatter_hidden_x_hidden_y_theme

plot_PBSIM3 <- ggplot(df_PBSIM3, aes(x = mean_qscore, y = Identity_Qscore)) +
  figure_4_point_layer(figure_4_colours[["PBSIM3"]]) +
  mean_qscore_x_scale() +
  identity_qscore_y_scale() +
  labs(x = "", y = "", title = "PBSIM3") +
  scatter_visible_x_theme

plot_SimLoRD <- ggplot(df_SimLoRD, aes(x = mean_qscore, y = Identity_Qscore)) +
  figure_4_point_layer(figure_4_colours[["SimLoRD"]]) +
  mean_qscore_x_scale() +
  identity_qscore_y_scale() +
  labs(x = "Mean quality score (per read)", y = "", title = "SimLoRD") +
  scatter_visible_x_hidden_y_theme

plot_real <- ggplot(df_Real_reads, aes(x = mean_qscore, y = Identity_Qscore)) +
  figure_4_point_layer(figure_4_colours[["Real reads"]]) +
  mean_qscore_x_scale() +
  identity_qscore_y_scale() +
  labs(x = "", y = "", title = "Real reads") +
  scatter_visible_x_hidden_y_theme

figure_4 <- make_tool_matrix(
  plots = list(
    plot_Badread, plot_LongISLND, NULL,
    plot_lrsim, plot_NanoSim, plot_real,
    plot_PBSIM3, plot_SimLoRD, NULL
  ),
  title = "Mean identity-derived quality score vs empirical quality score for each tool",
  align = "hv",
  axis = "tblr"
)

ggsave(
  "figures/figure_4.pdf",
  figure_4,
  height  = 160,
  width = 200,
  units  = "mm",
  device = "pdf"
)

figure_4
```

```{r}
files <- tribble(
  ~tool,        ~file,
  "Real reads", "results/real.qscore_counts.tsv",
  "Badread",    "results/Badread.qscore_counts.tsv",
  "LongISLND",  "results/LongISLND.qscore_counts.tsv",
  "lrsim",      "results/lrsim.qscore_counts.tsv",
  "NanoSim",    "results/NanoSim.qscore_counts.tsv",
  "PBSIM3",     "results/PBSIM3.qscore_counts.tsv",
  "simlord",    "results/simlord.qscore_counts.tsv"
)

per_base <- files %>%
  mutate(data = map(file, ~ read_tsv(.x, show_col_types = FALSE))) %>%
  unnest(data) %>%
  mutate(
    all_errors   = mismatch + insertion + deletion,
    total        = all_errors + match,
    error_rate   = all_errors / total,
    error_qscore = -10 * log10(error_rate)
  ) %>%
  filter(!is.na(total) & total > 0) %>%
  mutate(
    tool = factor(
      tool,
      levels = c("Real reads", setdiff(unique(tool), "Real reads"))
    )
  )

max_q <- max(per_base$qscore, na.rm = TRUE)

per_base <- per_base %>%
  mutate(tool = recode(
    tool,
    "simlord" = "SimLoRD",
    "real_reads" = "Real reads"
  ))

per_base_sim <- per_base %>% filter(tool != "Real reads")
per_base_real <- per_base %>% filter(tool == "Real reads")
```

```{r}
# Plot for the six simulation tools
p_sim_correct <- ggplot(per_base_sim, aes(x = qscore, y = match)) +
  geom_col(aes(fill = tool, colour = after_scale(fill)), width = 1, linewidth = 0.15) +
  facet_wrap(~tool, scales = "free_y", nrow = 3) +
  labs(x = "Q score", y = "", title = "A. Correct base Q score histogram") +
  supplementary_facet_theme +
  supplementary_qscore_x_scale() +
  count_y_scale() +
  scale_fill_manual(values = simulator_colours)

# Plot for real reads with a fake facet label on top
p_real_correct <- ggplot(per_base_real, aes(x = qscore, y = match)) +
  geom_col(fill = real_reads_colour, colour = real_reads_colour, width = 1, linewidth = 0.15) +
  facet_wrap(~tool, scales = "free_y", nrow = 3) +
  labs(x = "", y = "") +
  supplementary_facet_theme +
  supplementary_qscore_x_scale() +
  count_y_scale()

# Combine: sim facets on left, real reads + blank on right stacked
right_col <- plot_grid(
  NULL,
  p_real_correct,
  NULL,
  ncol = 1,
  rel_heights = c(0.73, 0.77, 0.50)
)

A <- plot_grid(
  p_sim_correct,
  right_col,
  ncol = 2,
  rel_widths = c(2, 1),
  align = "h",
  axis = "b"
)
```

```{r}
# Plot for the six simulation tools
p_sim_error <- ggplot(per_base_sim, aes(x = qscore, y = all_errors)) +
  geom_col(aes(fill = tool, colour = after_scale(fill)), width = 1, linewidth = 0.15) +
  facet_wrap(~tool, scales = "free_y", nrow = 3) +
  labs(x = "Q score", y = "Count", title = "B. Error base Q score histogram") +
  supplementary_facet_no_y_grid_theme +
  supplementary_qscore_x_scale() +
  count_y_scale() +
  scale_fill_manual(values = simulator_colours)

# Plot for real reads with a fake facet label on top
p_real_error <- ggplot(per_base_real, aes(x = qscore, y = all_errors)) +
  geom_col(fill = real_reads_colour, colour = real_reads_colour, width = 1, linewidth = 0.15) +
  facet_wrap(~tool, scales = "free_y", nrow = 3) +
  labs(x = "", y = "") +
  supplementary_facet_no_y_grid_theme +
  supplementary_qscore_x_scale() +
  count_y_scale()

# Combine: sim facets on left, real reads + blank on right stacked
right_col <- plot_grid(
  NULL,
  p_real_error,
  NULL,
  ncol = 1,
  rel_heights = c(0.73, 0.77, 0.50)
)

B <- plot_grid(
  p_sim_error,
  right_col,
  ncol = 2,
  rel_widths = c(2, 1),
  align = "h",
  axis = "b"
)
```

```{r}
p_badread_cal <- per_base_calibration_plot(
  data = per_base %>% filter(tool == "Badread"),
  colour = badread_colour,
  title = "Badread",
  plot_theme = scatter_hidden_x_theme
)

p_longislnd_cal <- per_base_calibration_plot(
  data = per_base %>% filter(tool == "LongISLND"),
  colour = longislnd_colour,
  title = "LongISLND",
  plot_theme = scatter_hidden_x_hidden_y_theme
)

p_lrsim_cal <- per_base_calibration_plot(
  data = per_base %>% filter(tool == "lrsim"),
  colour = lrsim_colour,
  title = "lrsim",
  y_label = "Identity-derived Q score",
  plot_theme = scatter_hidden_x_theme
)

p_nanosim_cal <- per_base_calibration_plot(
  data = per_base %>% filter(tool == "NanoSim"),
  colour = nanosim_colour,
  title = "NanoSim",
  plot_theme = scatter_hidden_x_hidden_y_theme
)

p_real_cal <- per_base_calibration_plot(
  data = per_base_real,
  colour = real_reads_colour,
  title = "Real reads",
  plot_theme = scatter_visible_x_hidden_y_theme
)

p_pbsim3_cal <- per_base_calibration_plot(
  data = per_base %>% filter(tool == "PBSIM3"),
  colour = pbsim3_colour,
  title = "PBSIM3",
  x_label = "Actual Q score",
  plot_theme = scatter_visible_x_theme
)

p_simlord_cal <- per_base_calibration_plot(
  data = per_base %>% filter(tool == "SimLoRD"),
  colour = simlord_colour,
  title = "SimLoRD",
  x_label = "Actual Q score",
  plot_theme = scatter_visible_x_hidden_y_theme
)

C <- make_tool_matrix(
  plots = list(
    p_badread_cal, p_longislnd_cal, NULL,
    p_lrsim_cal, p_nanosim_cal, p_real_cal,
    p_pbsim3_cal, p_simlord_cal, NULL
  ),
  title = "C. Per-base Q score calibration",
  align = "hv",
  axis = "tblr"
)
```

```{r, fig.height= 14, fig.width= 7}
supplementary_1 <- plot_grid(A, B, C, ncol = 1)

ggsave(
  "figures/figure_s1.pdf",
  supplementary_1,
  height  = 400,
  width = 200,
  units  = "mm",
  device = "pdf"
)

supplementary_1
```
